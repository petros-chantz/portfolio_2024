---

---

<section
  class="h-full fixed left-1/2 w-1/2 invisible py-6 flex bg-primary backdrop-blur-sm bg-opacity-10 items-center"
  id="alert"
>
  <div
    class="flex flex-col justify-between self-center w-full bg-white drop-shadow-2xl m-6 p-6 rounded"
  >
    <div class="flex flex-row items-start gap-3">
      <div class="flex justify-start mt-1">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="currentColor"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="white"
          class="w-8 h-8"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="m11.25 11.25.041-.02a.75.75 0 0 1 1.063.852l-.708 2.836a.75.75 0 0 0 1.063.853l.041-.021M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Zm-9-3.75h.008v.008H12V8.25Z"
          ></path>
        </svg>
      </div>

      <div class="flex justify-start">
        <p class="text-lg font-medium">
          Due to NDA reasons, I can’t show my work in the public domain.
          However, I do have a private portfolio with non-NDA work you can
          request.
        </p>
      </div>
    </div>

    <div class="flex flex-row gap-3 justify-end">
      <button type="button" class="btn-nothanks" btn-resolve-alert
        >Ok, maybe later</button
      >
      <a href="mailto:" class="btn-request" btn-resolve-alert
        >Request portfolio</a
      >
    </div>
  </div>
</section>

<script>
  import { isOpen } from "../store/store.js";

  isOpen.subscribe((open) => {
    if (open) {
      document.getElementById("alert")!.style.visibility = "visible";
    } else {
      document.getElementById("alert")!.style.visibility = "hidden";
    }
  });
</script>

<script>
  import { isOpen } from "../store/store.js";

  function openAlert() {
    isOpen.set(false);
  }

  const buttons = document.querySelectorAll("[btn-resolve-alert]");

  buttons.forEach((button) => {
    button.addEventListener("click", () => openAlert());
  });
</script>
